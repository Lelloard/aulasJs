// strin, number, undefined, null, bollean, symbol

const nome = 'lello';
const nome1 = 'lello';
const nome2 = 'lello'; //string
const num1 = 10; //number
const num2 = 10.52;

let nomeAluno; // undefined -> não aponta pra local nenhuma na memória
const sobrenomeAluno = null; // Nulo -> não aponta pra local nenhuma da memória
const aprovado = false; // Boolean = true, false(lógico)

const a = [1,2];
const b = a;
console.log(a)
